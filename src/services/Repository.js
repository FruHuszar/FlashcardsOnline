export default class Repository {
  lekerdez() {
    return Promise.reject(new Error("A lekerdez() nincs megvalósítva."));
  }

  letrehoz() {
    return Promise.reject(new Error("A letrehoz() nincs megvalósítva."));
  }

  modosit() {
    return Promise.reject(new Error("A modosit() nincs megvalósítva."));
  }

  torol() {
    return Promise.reject(new Error("A torol() nincs megvalósítva."));
  }

  hozzafuz() {
    return Promise.reject(new Error("A hozzafuz() nincs megvalósítva."));
  }

  felulir() {
    return Promise.reject(new Error("A felulir() nincs megvalósítva."));
  }

  csucsMentes() {
    return Promise.reject(new Error("A csucsMentes() nincs megvalósítva."));
  }
}
